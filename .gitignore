# Build artifacts
/target

# Local planning notes — never shared
AGENT.md
/plan

# Generated docs artifacts of `make docs` (regenerated per release, not
# reviewed in diffs); docs/CLI.md and docs/reference/ stay tracked.
docs/man/
docs/completions/
